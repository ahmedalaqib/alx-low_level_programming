#include <stdio.h>

/**
 * print_last_digit - Prints the last digit of a number
 * @n: Number to be passed 
 * Return: Last digit
 */

int print_last_digit(int n)
{
	int m;

	m = n % 10;

	return (m);
}
